# Node modules (already installed in container)
node_modules
npm-debug.log
yarn-error.log

# Build output (we only need fresh builds in Docker)
build

# Git
.git
.gitignore

# Docker files
Dockerfile
.dockerignore

# OS / IDE junk
.DS_Store
*.swp
*.swo
.vscode
.idea
